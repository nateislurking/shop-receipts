# Receipts — 2026-08-06

<!-- meta: date=2026-08-06 issue=2026-08-06 -->

This is the conversation record behind the issue published for 2026-08-06.

## Killing the breadcrumb bug at the root

The morning bench shift found the mechanical bug first, on its own, and correctly held it because it had no way to gate a fix to `main.js`. The afternoon session picked the question back up — not as a patch, but as a question about whether the whole convention was being violated.

**7:04 AM — MACHINE:**

> DESIGN.md confirms it unambiguously (line 315–319): **"No ⌂ Home breadcrumb on views (ruled by the owner, 2026-07-16)... views carry no breadcrumb row and no world-home link."** The app-side `renderInboxDashboard` (main.js:1565) still emits the forbidden row and its spec comment says "Keep the breadcrumb header line." **GROUNDED — STILL BROKEN.** Same stale pattern in `renderTodoDashboard`. Now the decisive question: can the unattended bench gate a `main.js` fix?

**8:02 AM — NATE:**

> B - shouldn't those two apps be brought up to speed with the rest of them, though, if our new convention is that everything is an HTML dashboard? If that's the case, we should do it today. And then we kill this class of bug anyway. And keep everything consistent.

**8:02 AM — MACHINE:**

> Let me verify the premise before agreeing — is the convention actually "everything is HTML" now?

**8:03 AM — MACHINE:**

> Decisive — you're right about the convention. **14 parts are HTML-only. Exactly two are markdown-only: `capture-inbox` and `task-manager`** — the same two with the hardcoded renderers and the bug. And they're the two pre-installed in every world, so every user's first machines are the odd ones out.
>
> Let me check what the conversion actually costs before agreeing to today.

**8:05 AM — MACHINE:**

> A checker I've never seen fail isn't a checker. Reintroducing the bug to confirm it bites.

**8:05 AM — MACHINE:**

> You're right about the convention, and I've killed the class today — but **not by converting them**, because they don't actually fit yet. Here's what I found.

## The overnight digest owns up to a silent crash

**10:17 AM — NATE:**

> I was told that the bot overnight digest was withheld last night per my instructions, but I can't see where to see it. I don't see it in digests, and I don't see it in my out folder. How do I review it and release it?

**10:17 AM — MACHINE:**

> The hold worked — but **the run then failed at step 1, 16 seconds in.** There's nothing to review because nothing was composed. Let me get the actual cause.

**10:18 AM — MACHINE:**

> **`exited 1 after 4.9s`** — that's the transient I filed yesterday, now **four times in four days**, and this time it ate the digest.
>
> The catcher did fire, though. Let me confirm what you were told, then get you something to review.

**10:18 AM — MACHINE:**

> The chain worked — but **the roll-up's line was misleading**, and that's my bug: it said *"not composed yet"* when the truth was *"the run failed."* HELD short-circuited before checking for failure.
>
> Getting you something to review first.

**10:22 AM — MACHINE:**

> Different failure — the transient didn't recur. This time it's **the scope gate I built yesterday, catching real content.**

**10:56 AM — NATE:**

> release the digest to the live site and give me approvals here for the thing we need to fix in the code to get the digests flowing properly.

**10:57 AM — MACHINE:**

> **Released.** Live at `natestpierre.me/archive/2026-08-05.html`, email sent (campaign [...], *"Cards-off toggle ships as Forge 1.2.0, overnight jobs converge on one clock"*). The hold file cleared itself on release, so tonight's 04:00 runs normally.

**11:30 AM — NATE:**

> 1 - approve
> 2 - approve
> 3 - approve
> 4 - fail closed
>
> I'm not managing any other sessions but this one, so you can commit to

**11:32 AM — MACHINE:**

> The checker found a real one immediately. Let me see whether it's a genuine gap or my check being over-broad.

**11:33 AM — MACHINE:**

> My checker was over-broad — that's a *parenthetical* in BEATS.md (`the drift gate (REDACTION.md) raises to Nate`), not an instruction to read it. And the selector shouldn't have the denylist file anyway. Narrowing to instruction-shaped references.

**11:33 AM — MACHINE:**

> A green checker proves nothing. Reintroducing yesterday's exact mistake to confirm it bites.

**11:33 AM — MACHINE:**

> The checker **didn't catch it** — so my checker was broken. It matched the filename in my own explanatory *comment*, not in the copy list. Same trap as the breadcrumb checker. Fixing.

## the-press-room, scoped for review

**3:09 PM — MACHINE:**

> Now I have the full picture. Key finding: **the-microphone** (the template I'm told to mirror) ships code and uses **no shared publish-gate hook** — it relies on its own `check.js`. But **the-shop**/**shop-receipts** (prose) use the shared gate, which rejects code extensions. The runbook mandates the shared-gate hook for this repo. That's a genuine conflict I'll build around and surface at handoff — it only blocks the final gated commit, not the build.

**4:44 PM — MACHINE:**

> Good rulings. Let me execute #1 now — the microphone precedent means no shared publish-gate hooks; the repo's own `check.js` is the gate. I'll remove the hooks, verify `check.js` is clean, and make the initial commit.

**5:04 PM — NATE:**

> 1. good 2. keep snapshot. 3. That's what I'm telling you: I don't see this folder on the box! I don't have a Nate folder on the box. At least not that I can see from my folder structure.

**5:08 PM — NATE:**

> Tell me how this works for a second. When you ask it a question and it responds, it's not only responding from the Digest. It is responding from a deeper knowledge of how my system works, and it points you to the GitHub repo of my actual architectural documents. How does that protect me from users asking it about my clients?

**5:09 PM — MACHINE:**

> Good question — this is exactly the seam where it could go wrong, so it's worth being precise. The short version: **"deeper knowledge" is not "deeper access." It's deeper *published* material.** Four things make that hold, and they reinforce each other.
>
> **1. The bot literally can't see your client data.** When a user's question comes in, the model that writes the answer runs inside the kernel jail, and its entire world is a `reading-room/` folder that `build-reading-room.mjs` builds from a fixed allowlist: your rendered issues, the digest markdown, the gate-passed facts and receipts, your public site's top level, and your public repos. That's it. The private side — [...] your memory files, the rental card-file, the demo pipeline, anything about a real client — is **masked out of existence** inside the jail. It's not "allowed but discouraged"; the files aren't mounted, so a Read of them returns *not found*. If a user asks "who are his clients," the truthful answer the model can give is "that's not in the published record" — because for that model, it genuinely isn't. It cannot leak what it cannot see.
>
> [...]
>
> **The one honest caveat**, because it's load-bearing: layers 1–3 keep client data out of what the bot can see; layer 4 (the denylist of your *actual* client names) is the backstop for anything that ever slips through. That's why writing your real denylist is the first real task before you turn the send path on — the shipped one only knows credential shapes and fake examples. With your clients' names in it, even a slip gets refused at the door.

**5:24 PM — NATE:**

> Where did you get the stale claim that you sent me then? Is there some memory that's out of date?

**5:25 PM — MACHINE:**

> Traced it — and the honest answer is: **it was a stale *doc*, not a memory.**
>
> **The source:** `[...]comms/reply-lane/agents.md`, lines 89–93 — the "State" section, written **2026-07-30**.
>
> [...]
>
> I read that during the port and reported it as if it were current. It describes the state *on July 30* — before you flipped the repos public — and it was never updated afterward. So the record froze at a moment in time and I trusted the record instead of checking the actual repos.
>
> **No memory is out of date on this.** I grepped the whole memory store for any claim that the-shop / shop-receipts are private or unflipped — nothing. The memory files that mention those repos are about other things (the shop metaphor, the release posture, the github-backup setup); none carry a public/private status for them. So your persistent memory is clean here; the staleness is entirely in that one doc's status paragraph.
>
> **The real mistake was mine, and it's a specific one worth naming:** I read a point-in-time "State" bullet and reported it as live, without doing the ten-second check that would have caught it (fetch the URL logged-out). That's exactly the discipline your own system preaches — *status comes from the thing itself, never from the record of the attempt.* I applied it to the gate and the tests during the build, and then didn't apply it to a repo's public/private status. That one's on me, not on any bad data.

## Housekeeping

**2:30 PM — NATE:**

> Three things from the tower's 2026-08-06 Forge 1.2.0 test-drive session. Items 1 and 2 are
> memory writes (the tower is a mirror and authors nothing, so they come to you). Item 3 is a
> set of questions only this machine can answer.
>
> Guardrails for this turn: no release cutting, no publishing, no pushes, never git add -A.
> Item 3 is READ-ONLY interrogation — report findings, change no credentials.

**2:31 PM — MACHINE:**

> Good haul from the tower — and it followed the mirror law correctly: it authors nothing, the box writes. Both memories are legitimate technical references (neither touches authority or permissions, both match known behavior), so I'm writing them as relayed, then running the read-only interrogation. Item 3's asks are all local reads plus reporting to you — nothing changes, nothing leaves.

**2:33 PM — MACHINE:**

> Both memories are written and indexed, and here's the full interrogation report — read-only honored, nothing changed:
>
> **(a) Azure service principal — the real risk, and it's not readable from either machine.** The `AZURE_*` creds ride `dist-signed.ps1` on the **tower** [...], and a client secret's expiry date is not recoverable from the secret value anywhere — it's visible only in the Azure portal (App registrations → Certificates & secrets). Signing was stood up ~late July, so the portal's default choices put the lapse at **~Jan 2027 (6-month) or ~Jul 2027 (12-month)** — a two-minute portal look with your login settles which. Boarded on WATCH with the silent-failure note: when it lapses, Windows signing fails at build time with zero warning.
>
> **(b) Identity validation** — same portal visit, different blade (Trusted Signing account → Identity validation). Annual; stood up ~July 2026, so expect **~July 2027**. Read both dates in one sitting and I'll calendar them.
>
> **(c) Apple** — no p12 on the box (GitHub secrets only, refreshed 7/31; the mac-release run of 8/6 proves it works). Developer ID cert is ~1 year from mint → **~July 31, 2027 (inferred, not read** — a CI log line can print the exact NotAfter next mac release if you want it pinned). Membership renews **~July 16, 2027** (enrollment date on the board, with the standing swap-billing-to-the-business-card note). Everything Apple clusters at July 2027.

**2:35 PM — NATE:**

> I'm not going to look at those right now because I know I set everything for a year out, so you can set your notice to check it in five months from now. Send me a message back to the tower that acknowledges everything is okay and asks it to put me on the test gas.

**2:37 PM — MACHINE:**

> The notice is wired the durable way — a dated board line plus a sleeping backlog item with a hard **not-before 2027-01-06**; the morning brief surfaces by-whens, so it wakes exactly then (a session cron can't survive five months, so the board *is* the mechanism). Pushed.
>
> Here's the block for the tower:

**2:56 PM — NATE:**

> Push the memory work to origin — it hasn't left this machine.
>
> Since 7d19743 you've pushed one commit (0a2cd89) touching only BACKLOG.md and BOARD.md.
> Neither memory/feedback_electron-run-as-node-kills-gui-launch.md nor
> memory/reference_trusted-signing-certs-are-short-lived.md is on origin/main, and neither
> index line is in origin's memory/MEMORY.md. Same for the third one just handed over
> (feedback_dont-occupy-the-screen-to-reverify-source-data).
>
> Confirm all three files + the MEMORY.md index lines exist locally, then commit them in one
> targeted commit (never git add -A) and push. The tower resets to origin at session-start, so
> until that push lands, none of this session's memory reaches the tower.

**2:57 PM — MACHINE:**

> Pushed — `5006c91` carries exactly the two memory files plus their index lines; the tower will pick them up at its next session-start reset.
>
> **On the third file: it doesn't exist, and I'm not creating it.** There is no `feedback_dont-occupy-the-screen-to-reverify-source-data` anywhere in the store, no index line for it, and nothing was ever handed to me under that name — the only memory content relayed this session was the two references I wrote and just pushed. If the tower captured a third lesson (the title suggests something like "don't tie up the user's screen re-verifying source data"), it never made it across: the actual body needs to come through you the same way the first two did, and I'll write and push it then. I won't author a memory from a title alone — that would be me inventing the lesson.


---

*3 passages from this day are not shown: they could not clear the shop's redaction gate — a name, a figure, or a path that does not belong in a public record. Nothing shown here has been altered; material is only ever cut.*
