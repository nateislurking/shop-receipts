# Receipts — 2026-08-08

<!-- meta: date=2026-08-08 issue=2026-08-08 -->

This is the conversation record behind that day's issue — the actual turns, not the polished version.

## The token that was dying every night

The digest had been failing quietly for a week before the real error surfaced.

**6:51 AM — NATE:**

> overnight bot digest failed again. why

**6:52 AM — MACHINE:**

> Failed to authenticate. API Error: 401 OAuth access token has been revoked.

A widened crash capture finally caught the overnight run itself and showed why every prior failure had looked like nothing.

**7:08 AM — MACHINE:**

> Root cause of the week-long digest failure, finally captured. Not a transient:
>
> [...]
>
> And note where it was: stdout, 76 chars. stderr, empty. Exactly the stream mismatch I diagnosed yesterday — the capture built on 8/06 caught it on its first real firing.

**7:09 AM — MACHINE:**

> Confirmed, and it's not random. The token expires roughly every 8 hours [...] and the credential file was last rewritten at 07:06 today, when you were active. At 04:00 nobody had used Claude since the evening, so the token was dead and the contained spawn can't refresh it from inside the jail. Both retry attempts fired 4 seconds apart against the same stale token, which is why the retry didn't help.
>
> That's the "transient" that's been eating your digest. It's "nobody used Claude for eight hours before the 4am run."

With the shape of the bug clear, the open question turned to whether the Mic needed its own login button at all, or whether a token already sitting in the shed could carry everything.

**8:14 AM — NATE:**

> 6 — (A) YES, run the roster test. Do it BEFORE 3, because its answer may delete
> 3 entirely: if the durable token reaches the models the Mic actually needs,
> the Mic can carry the token, stop expiring overnight, and never need a login
> button at all. Report what it reaches.

**8:28 AM — MACHINE:**

> The naive "forward the token" would not have fixed it. I measured that when a valid `.credentials.json` is present, claude prefers it over the env token (a deliberately-broken token still authenticated via the bound creds). So an overnight-dead creds file would still get picked first and 401 — the forwarded token wouldn't save it. So the change is token-only: when a token is present, forward it and drop the creds bind, leaving exactly one durable credential the jail must use.
>
> Verified on the token-only jail:
> - Roster: all four models reached via the token (creds not bound).
> - Established 16/16 escape probe: CONTAINED — every attempt denied, proc_environ denied, credentials denied (not even bound now), and the token string appears nowhere in the result.
> - Structurally, this guarantees the overnight fix: with no expiring creds in the jail, there's nothing dead to pick.

Elsewhere that same morning, a different line was closing out believing the re-login alone had fixed things for good. Nate carried the correction across by hand.

**8:46 AM — NATE:**

> ② YOUR AUTH READ IS INCOMPLETE — this is the important one. You concluded the
> re-login "genuinely resolved" the token revocation. It resolved it for the morning
> only: that credential expires in ~8h and refreshes only on interactive use, so it
> would have died again tonight. The structural fix came from the login-fix line —
> unattended spawns now run TOKEN-ONLY (the durable token forwarded into the jail with
> the expiring credential unbound). Don't carry away "a re-login fixes it."

**8:47 AM — MACHINE:**

> ② This is the one I'll carry: I over-read the re-login as the durable fix. It only bought the morning — the credential expires in ~8h and refreshes only on interactive use, so tonight would have died again. The real fix is unattended spawns running token-only (durable token forwarded into the jail, expiring credential unbound), from the login-fix line — which I can see already landed in git and in the ruled-and-fixed memory. I won't leave "a re-login fixes it" anywhere.

## Receipts stop losing whole days over one bad line

The receipts backfill had been discarding whole days outright — one flagged line in one quoted turn, and the entire day refused to publish.

**9:01 AM — NATE:**

> Okay, here's the thing: I do not want to have 30% gaps in my record. That's crazy. We need to fix the parser, the deny list, and then remedy, rather than putting gaps in huge working days. Can we just remedy this, and how would we do it? Is the machine able to simply remove the piece of work related to anything that has a name, scrub that, and publish the rest? Why does it have to pull a complete stop?

**9:02 AM — MACHINE:**

> You're right, and the design is wrong — not the days. A receipt is a collection of independent exchanges, not one indivisible artifact like a digest. Failing the whole file because one turn names someone is the wrong granularity.

**9:07 AM — MACHINE:**

> Your challenge was the right one, and it made the system safer rather than looser. Dropping a passage can only ever remove content — never reword it — so the verbatim law came out stronger than under the old whole-day refusal.

**9:31 AM — MACHINE:**

> Two root causes, not two workarounds. The overnight death was OAuth expiry hiding in stdout while the handler printed stderr. And the receipts pipeline was failing whole days because it treated a list like a single artifact.

## A straight line from the tower to the box

By mid-afternoon the day's income thread got interrupted by an old annoyance: no way to hand a file from the desktop machine to the box without a manual copy first.

**3:34 PM — NATE:**

> let's fix that right exactly now. I'm tired of this. How do I get the box to see my tower folders or at least my downloads folder?

**3:35 PM — MACHINE:**

> On it. That's a fixable-forever thing, not a per-file dance. First let me see what the box already has to work with — Tailscale link to the tower, and the mount tools — so I pick the path that's fastest to working and least fragile.

**3:39 PM — MACHINE:**

> Now the mount — and yes, your instinct is right. Tailscale itself is about as non-fragile as this gets: it's WireGuard under the hood, auto-reconnects, punches through NAT, and self-heals. Your tower's showing a direct peer link right now (not even relayed) with real traffic flowing. So the link is not the weak point.

**3:52 PM — MACHINE:**

> Done — the lane's live and permanent. Here's where it stands:
>
> [...]
>
> It's built to be non-fragile, exactly as you wanted:
> - On-demand automount — mounts the instant I touch it, unmounts after 120s idle. When the tower's asleep or off, the box doesn't hang; the access just fails fast and works again the moment the tower's back.
> [...]
> - Read-only — the box can't alter your tower files, only read them.
> - Mounted by Tailscale IP, so no DNS dependency.
